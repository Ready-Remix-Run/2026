[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "crayon-commotion"
version = "0.1.0"
description = "Generator for collaborateive image decoding activities"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]